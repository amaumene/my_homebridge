/**
 * Platform identifiers shared across the plugin.
 *
 * PLATFORM_NAME must match the `platform` value used in the Homebridge
 * `config.json` and the `pluginAlias` in `config.schema.json`.
 * PLUGIN_NAME must match the `name` field in `package.json`.
 */

export const PLATFORM_NAME = "AirCloudHome";
export const PLUGIN_NAME = "homebridge-aircloudhome";
